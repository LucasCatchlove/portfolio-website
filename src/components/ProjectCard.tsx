
type ProjectProps = {
    title: string;
    description: string;
    technologies: string[];
}

const ProjectCard = (data: ProjectProps) => {
    return <div>
        <div></div>
        
    </div>

    
}

export default ProjectCard
//fetch data server side

